# AstraVaani Backend Environment Variables

# Application
APP_NAME=AstraVaani
DEBUG=true
SECRET_KEY=change-this-to-a-secure-random-string

# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/astravaani

# CORS (comma-separated origins)
CORS_ORIGINS=["http://localhost:3000"]

# JWT
JWT_SECRET_KEY=change-this-to-a-secure-jwt-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# LLM Configuration
LLM_PROVIDER=anthropic  # or "openai"
ANTHROPIC_API_KEY=sk-ant-api...

# Tier-specific Model Configuration (Anthropic Claude)
# Use -latest aliases for automatic version updates
# Free tier: Fast, cheap model (~₹0.80/chat)
LLM_MODEL_FREE=claude-3-5-haiku-latest
# Starter tier (₹99): Better quality responses (~₹2.50/chat)
LLM_MODEL_STARTER=claude-3-5-sonnet-latest
# Pro tier (₹699): Best quality + validation (~₹7/chat)
LLM_MODEL_PRO=claude-3-5-sonnet-latest
LLM_MODEL_PRO_VALIDATOR=claude-3-opus-latest

# Cost per chat:
# - Free: ~₹0.80 (Haiku only)
# - Starter (₹99): ~₹2.50 (Sonnet only)
# - Pro (₹699): ~₹7.00 (Sonnet + Opus validator)

# Razorpay Payment Gateway (Indian payments)
# Get keys from https://dashboard.razorpay.com/app/keys
RAZORPAY_KEY_ID=rzp_test_...
RAZORPAY_KEY_SECRET=your_razorpay_key_secret
RAZORPAY_WEBHOOK_SECRET=your_razorpay_webhook_secret

# App URLs
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:8000

# Google OAuth (get from Google Cloud Console)
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Email (Resend)
# Get API key from https://resend.com/api-keys
# IMPORTANT: You must verify your domain at https://resend.com/domains
# The test domain (onboarding@resend.dev) only sends to your own email
RESEND_API_KEY=re_xxxxxxxxxx
FROM_EMAIL=noreply@astravaani.com
